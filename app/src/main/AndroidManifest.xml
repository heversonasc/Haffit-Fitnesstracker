<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.heversonasc.haffit">

    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitnessTracker">
        <activity android:name=".tipsHealth"></activity>
        <activity
            android:name=".Splash"
            android:exported="true"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WaterActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".tmbActivity" />
        <activity
            android:name=".ListCalcActivity"
            android:parentActivityName="co.heversonasc.haffit.MainActivity" />
        <activity android:name=".ImcActivity" />
        <activity
            android:name="co.heversonasc.haffit.MainActivity"
            android:exported="true"></activity>
    </application>

</manifest>